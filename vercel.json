{
  "framework": "nextjs",
  "buildCommand": "npm install --force && npx next build",
  "outputDirectory": ".next",
  "installCommand": "npm install --force"
}
git add vercel.json
git commit -m "Force npm install in vercel.json"
git push origin main
